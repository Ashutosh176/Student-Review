# Copy to .env.local for local overrides. Not needed in dev (Vite proxies /api to
# the backend per vite.config.ts). Set this in production if the frontend and
# backend are deployed to different domains.
VITE_API_BASE_URL=/api
